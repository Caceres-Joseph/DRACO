

$*Este es un 
    comentario
        multilinea*$
        
        
$$comentario linea

If (a < b) {
    $$ instrucciones
    print ("Dibujara un cuadrado de 100 pixeles");
} elif (a < b) {
    $$ instrucciones
    print ("Dibujara un cuadrado de 100 pixeles");
} elif (a < b) {
    $$ instrucciones
    print ("Dibujara un cuadrado de 100 pixeles");
} if not {
    $$ instrucciones
    print ("Dibujara un cuadrado de 100 pixeles");
}
